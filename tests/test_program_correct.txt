prog TestProgram;

int i;
int j;
int k;
int sum;
int result;

float x;
float y;
float average;

bool flag;
bool condition;

int arr[10];
float data[5];

type Counter int;
type MyFloat float;

Counter count;
MyFloat value;

main {
    /* 
     * Init
     * Comment
     */
    
    sum = 0;
    flag = true;
    x = 1.5;
    y = 2.5;
    

    for (int n = 0; n < 10; n = n + 1) {
        sum = sum + n;
    }
    
    i = 0;
    for (i = 0; i < 5; i = i + 1) {
        arr[i] = i * i;
    }
    
    result = 0;
    for (int row = 0; row < 3; row = row + 1) {
        for (int col = 0; col < 3; col = col + 1) {
            result = result + row * col;
        }
    }
    

    k = 0;
    flag = true;
    for (k = 0; flag && (k < 5); k = k + 1) {
        flag = k < 3;
    }

    average = 0.0;
    for (int idx = 0; idx < 5; idx = idx + 1) {
        data[idx] = idx + 0.5;
        average = average + data[idx];
    }
    average = average / 5.0;
    

    count = 0;
    for (count = 0; count < 100; count = count + 10) {
        value = count + 0.1;
    }

    j = 10;
    k = 20;
    result = (j + k) * 2 - j / 2;

    condition = (i < j) && (j < k);
    flag = !condition || (sum > 100);
    
    // One line comment
}
